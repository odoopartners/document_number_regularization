<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="account_move_view_form_document_number_regularization" model="ir.ui.view">
        <field name="name">account.move.form.document.number.regularization</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='other_info']" position="inside">
                <group
                    string="First Document Number Update"
                    invisible="move_type not in ('in_invoice', 'in_refund')"
                >
                    <field name="previous_document_number"/>
                    <field name="document_number_regularization_date"/>
                    <field name="document_number_regularized_by_id"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
